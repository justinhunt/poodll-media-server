{
    # Let's Encrypt account email (required for cert issuance)
    email {$ADMIN_EMAIL}
}

# HTTPS: token endpoint + test UI
{$MEDIA_DOMAIN} {
    reverse_proxy test-client:8000
}

# WSS: LiveKit WebSocket signaling
{$MEDIA_DOMAIN}:7880 {
    reverse_proxy livekit-server:7880
}
